#include <iostream>
#include <string>

using std::cin;
using std::cout;
using std::endl;
using std::string;

int main(int argc, char const *argv[])
{
    string feed = "";
    while (getline(cin, feed))
    {
        cout << feed;
        cout << endl;
    }
    return 0;
}
